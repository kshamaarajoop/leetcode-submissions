class Solution:
    def majorityElement(self, nums: list[int]) -> int:
        hSet = {}
        for i,n in enumerate(nums):
            hSet[n] = hSet.get(n,0) + 1
            if hSet[n]>=len(nums)/2:
                return n
            
        
